# Instalar depenências do sistema

    npm init -y
    npm install express
    npm install body-parser
    npm install mongoose
    npm install bcryptjs
    npm install jsonwebtoken

    npm install fs
    npm install path

# 